#Converter o valor de Dólares em reais

valor = float(input('Digite valor em $: '))
taxa = float(input('Taxa de conversão para R$: '))

total = valor + taxa
print(f'A soma dos números é: {total:.2f}')